import { FC } from 'react';

import inputLayout from './input.module.css';

const Input: FC = () => {

  return null;
}

export default Input;

/*<input
className={inputLayout.input}
onChange={handleChange}
icon={'EditIcon'}
type={'text'}
placeholder={'Имя'}
value={values.name}
name={'name'}
size={'default'}
/>*/
